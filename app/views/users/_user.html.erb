<li>
  <%=  user.email %>
  <% if current_user.teacher?  %>
    <% unless @meta %>
      <%= link_to "Destroy", user, method: :delete, data: { confirm: "You sure?" } %>
    <%end%>

    <%if @meta%>
        <%= link_to "Add to group" , user_update_task_path(user,@group), method: :put, data:{confirm: "You sure?"} %>
    <%end%>
  <%end%>
</li>